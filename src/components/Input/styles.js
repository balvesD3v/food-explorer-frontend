import styled from "styled-components";
import { Link } from "react-router-dom";

export const DivStyled = styled(Link)`
  text-align: center;
`;

export const InputStyled = styled.input`
  background-color: transparent;
  border: none;
  color: #fff;
  cursor: pointer;

  font-family: "Poppins";
  font-size: 14px;
  font-style: normal;
  font-weight: 500;
  line-height: 24px;
`;
